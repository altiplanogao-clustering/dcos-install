---

resource_cache: "{{ playbook_dir | dirname }}/.resources"

cluster_name: "dcos_cluster"

bootstrap:
  user: deployer
  group: wheel
  password: deployer
  working_dir: /dcos

# docker:
#   user: 